stormblockmk (the RouterOS containment profile) lives in its own project:
https://github.com/glennswest/stormblockmk — it consumes stormblock as an
unmodified library (default features). Design context:
https://github.com/glennswest/stormblock-registry (docs/spec.md).
